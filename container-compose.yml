services:
  nginx:
    build:
      dockerfile: Containerfile
      target: nginx
    volumes:
      - ./ssl:/etc/ssl/nginx
      - ./include.d:/etc/nginx/include.d
    ports:
      - 80:80
      - 443:443
      - 465:465
      - 995:995
