# Root context snippets

files with `.conf` extension in this directory will be included in the root nginx context after
the default global options, but before the top level blocks (events, http, mail)
